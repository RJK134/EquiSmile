<!--
  Standard PR shape for EquiSmile. Keep this template populated; reviewers
  scan top-to-bottom. The five-check gate (lint, typecheck, prisma validate,
  test, build) MUST be green locally before marking ready-for-review.

  Final merge always belongs to a human (Richard or Freddie) per CLAUDE.md.
-->

## Summary

<!-- One sentence: what this PR does and why. -->

## Scope

<!--
  Bullet what's IN this PR. Per the CLAUDE.md PR-batching rule, one branch =
  one PR; batch related fixes in here rather than opening follow-ups.
-->

-

## Files touched

<!-- Brief list of paths modified + new. -->

## Test plan

<!-- Bulleted markdown checklist. Include both automated + manual checks. -->

- [ ] `npm run lint` clean
- [ ] `npm run typecheck` clean
- [ ] `npx prisma validate` (with stub `DATABASE_URL=postgresql://test:test@localhost:5432/test`)
- [ ] `npm run test` — all suites green
- [ ] `npm run build` — production build succeeds
- [ ] Mobile layout checked at 390 px width
- [ ] Manual:

## Risk

<!--
  Surface anything reviewers should look hard at. Examples:
   - "Touches WhatsApp webhook intake — production messaging exposure."
   - "Adds nullable column; safe to roll back."
   - "Demo-mode-only endpoint; gated by DEMO_MODE check."
  If genuinely no risk: "Additive, reversible, demo-mode-only."
-->

## Stop-gate

<!--
  Does this PR need an explicit human pause before merge? Examples:
   - "Auth path change — review-bot worth, do not auto-merge."
   - "Schema migration — confirm DATABASE_URL backup before deploy."
  If none: "None — safe to merge after CI green + reviewer approval."
-->

## Verification (post-merge)

<!--
  How does the operator confirm the change works on Vercel preview /
  production? curl commands, page paths, expected log lines, etc.
-->

---

<!-- Generated by Claude Code session. Append after the auto-link line below. -->
